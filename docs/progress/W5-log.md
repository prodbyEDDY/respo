# W5 «Production Core» — рабочий лог

> Ветка `w5-core` (от `main` = 6d80e87). План: [`../superpowers/plans/2026-09-05-w5-production-core.md`](../superpowers/plans/2026-09-05-w5-production-core.md).
> Одна запись на задачу, чтобы прерванную сессию можно было продолжить с середины.
> Финальный отчёт волны — `W5-production-core-2026-09-XX.md` (Task 11).

## Task 1 — Emulation pack ✅

- **Сделано:** `shared/emulation.ts` (нouns: профиль, пресеты сети как в DevTools, 8 городов, 11 локалей, 11 таймзон, гарды/парсеры), `main/emulation.ts` (EmulationManager: глобальный профиль + пер-девайс override vision, регистрация вьюшек по образцу Inspector, restore из persistence **до** первой вьюшки), `CDPController.applyEmulation` (диффит по группам — одна CDP-команда на изменённую группу; replay после реаттача; `Accept-Language` едет в том же `Network.setUserAgentOverride`, что и UA — второй вызов перетёр бы первый), IPC `emulation:set` / `emulation:set-device-vision` / `emulation:get` с валидацией, слайс `emulation` в документе (field-level repair, `SCHEMA_VERSION` не бампал — «отсутствует = дефолты», как у `layout`/`devtools`; бамп сбросил бы документ пользователя в бэкап ради опционального поля), стор `stores/emulation.ts`, кнопка **Emulate** с точкой-бейджем слева от SYNC + popover (`EmulatePopover.tsx`), shadcn `ui/select.tsx`, общий `common/Segmented.tsx` (SettingsDialog переведён на него).
- **Проверено фактически (Chromium 152.0.7977.54):** все 7 типов `setEmulatedVisionDeficiency` поддерживаются; `Network.emulateNetworkConditions` работает **без** `Network.enable` (offline валит загрузку всех 5 вьюшек); все overrides живут в CDP-сессии и **переживают навигацию/reload без реаппликации** (только реаттач требует replay — сделан); мусорный timezone Chromium отвергает («Invalid timezone id» → лог, остальное применяется), мусорную locale — молча принимает (поэтому валидация BCP 47 на нашей стороне обязательна). Offline не действует на `file://` (загрузчик файлов минует network service) — e2e поднимает локальный http-сервер.
- **UX-решения:** выбор города в Location также ставит таймзону (строка Time zone показывает результат); Off у Location чистит только координаты; при активной локации — подсказка «Sites still have to ask for permission» (разрешение остаётся «спрашивать»). Reset all — одна кнопка в шапке popover, disabled когда нечего сбрасывать.
- **Отложено:** произвольная локаль/таймзона текстом (только пресеты); e2e на geolocation (нужен allow разрешения — вручную проверено `setGeolocationOverride` принимается, юниты покрывают параметры); vision e2e — в Task 5.
- **Цифры:** юниты 947 (shared+main) + 393 (renderer) — все зелёные; новых: `shared/__tests__/emulation.test.ts`, `main/__tests__/emulation.test.ts`, `stores/__tests__/emulation.test.ts`, +11 в cdp-controller, +8 в validate, +8 в persistence-types. e2e `emulation-pack.spec.ts` 1/1 (dark/light, reduced motion, print, de-DE → `navigator.language`+`Intl`+`Accept-Language` на всех 5 запросах, Asia/Tokyo, reload, offline → 5 failed, reset, рестарт → первый же запрос уже fr-FR и dark).
- Коммит `c2c709f`.

## Task 2 — UA Client Hints ✅

- **Сделано:** `shared/client-hints.ts` — `clientHintsOf(spec)` выводит platform / platformVersion / architecture / bitness / model / mobile / brand из UA-строки (Android: версия и модель из скобок, `Build/…` отрезается, arch/bitness пустые как у настоящего Chrome on Android; Windows/macOS: замороженные UA не несут реальную версию ОС — hint отдаёт `15.0.0`; CrOS/Linux; Edge → бренд «Microsoft Edge»); `userAgentMetadataFor(hints, engineVersion)` — brands + fullVersionList (Chromium, бренд, GREASE «Not_A Brand»). `CDPController` получил опцию `chromiumVersion` (по умолчанию `process.versions.chrome`) и шлёт `userAgentMetadata` в том же `Network.setUserAgentOverride`.
- **Решения (отклонения от буквы плана):** (1) метаданные **выводятся из UA-строки**, а не хранятся полями `DeviceSpec` — иначе каталог и кастомные девайсы (у которых редактируется только UA) должны были бы быть правыми дважды; «дефолты по типу» получаются через дефолтный UA типа. (2) Major-версия брендов берётся из UA-строки (`Chrome/139`), а `process.versions.chrome` используется для full version только при совпадении major: страница, сравнивающая `Sec-CH-UA-Full-Version-List` с `navigator.userAgent`, должна видеть один браузер. Каталожный `CHROME` будет поднят до 152 в Task 3. (3) Safari/Firefox/CriOS-UA — **без** metadata: Chromium тогда не отдаёт `navigator.userAgentData` вовсе (проверено e2e: iPhone/iPad → `uaData === null`), что и есть честное поведение Safari.
- **Цифры:** +16 юнитов `client-hints.test.ts`, +3 в cdp-controller; e2e `emulation.spec.ts` расширен (Pixel → Android/15.0.0/«Pixel 8»/mobile, Windows/macOS десктопы, Safari-девайсы без hints) 1/1.
- Коммит `c038605`.

## Task 3 — Каталог устройств 110 ✅

- **Сделано:** перенос данных делегирован субагенту (`model: "sonnet"`, точное ТЗ: источник — Chromium DevTools `EmulatedDevices.ts` @ `036dd84b` BSD-3, + публичные спецификации вендоров; responsively-app — запрещён; 38 старых id/метрик неизменны). Результат 38 → **110** записей (+72: iPhone 8…17 Pro Max/Air, Pixel 3…10 + Fold, Galaxy S8…S25 Edge, Z Flip/Fold 6, OnePlus, Nothing, iPad 10/Air M3, Tab S9 Ultra/S10+, Pixel Tablet, Fire/Nexus, Surface Pro 9/11, MacBook Air/Pro M4, iMac, Chromebook (`CROS_UA`), мониторы до 4K). Константы: `CHROME=152.0.0.0` (= движок, см. Task 2), `IOS=26_0`, `ANDROID=16`.
- **Проверено мной:** `git diff` — старые 38 строк байт-в-байт (кроме констант); тесты каталога переписаны (≥ 90, W1-устройства на месте с теми же метриками, линейки 2025–26, dpr ∈ [1,4], Surface — по имени, а не по списку id); `client-hints.test.ts` проходит по всему каталогу (все Chrome-UA дают platform ≠ '', Safari — null). Миграция id не нужна — ни один id не менялся.
- **Атрибуция:** `NOTICE.md` (BSD-3 Chromium DevTools + пресеты сети), `docs/modules/device-catalog.md` (источники, правила стабильности id, список приближённых устройств).
- **Отложено:** уточнение метрик устройств, взятых по плотностным бакетам (список в доке модуля), когда они появятся в Chromium.

## Task 4 — Надёжность страницы ✅

- **Сделано:** `LoadState 'crashed'` (`render-process-gone` в `watchLoadState`, латч до следующей навигации) → ViewManager прячет вьюшку как при `failed`, `crashed`-флаг; оверлей **PageCrashed** с кнопкой Restart (`view:restart` → `wc.reload()`; CDP-сессия остаётся на `webContents`, Chromium восстанавливает эмуляцию в новый процесс — e2e: после Restart iPhone снова 393/iPhone-UA). Reload всех **пропускает** упавшую вьюшку (её возвращает только Restart). `nav:reload` получил `ReloadRequest {deviceId?, ignoreCache?}` (валидация), kebab **⋯** девайса: Reload / Reload ignoring cache / Scroll to top (`view:scroll-to-top` → `scrollToRatio(0,0)`) / Copy URL (clipboard рендерера + notice). Retry на карточке ошибки теперь перезагружает **этот** девайс. Хоткеи `Mod+R` / `Mod+Shift+R` (`useNavHotkeys`). **Popups:** `popupDecision(url, isLead)` в `security.ts` — только лидер (та же election, что у sync), только http(s), окно с sandbox/contextIsolation/без node/партиция `persist:respo`; последователи — deny **молча** (раньше всем открывался внешний браузер — 5 вкладок на один клик); попап-из-попапа deny; попапы закрываются с канвасом.
- **Проверено фактически:** mobile-эмуляция Chromium **не рисует скроллбары вовсе** (пиксельная проба `Page.captureScreenshot` на tall.html: iPhone — 0 не-белых пикселей у правого края сразу после скролла и через 2 с, `clientWidth` 393 = полная ширина; десктоп — классический 15px скроллбар, `clientWidth` 1425). → insertCSS `::-webkit-scrollbar{display:none}` и тумблер «Hide mobile scrollbars» **не нужны** — пропущено, спека §5.1 будет приведена к факту в Task 11.
- **Грабля (в CLAUDE.md):** `webContents.executeJavaScript` на только что упавшем рендерере может **зависнуть навсегда** (e2e висел 2 мин) — проверять `wc.isCrashed()`. Плюс: `rmSync` временного профиля на Windows ловит EBUSY на БД сессии — `ownProfile` теперь с `maxRetries` и предупреждением вместо падения (закрывает follow-up W4).
- **Цифры:** юниты +31 (view-manager 8, view-backend 3, security 10, validate 3, navigation 4, nav-hotkeys 5); e2e `reliability.spec.ts` 1/1 (краш → карточка только у жертвы, `isCrashed()` ровно у одной, остальные 4 живы; Restart → ready + эмуляция; клик в лидере → ровно 1 окно-попап c device-партицией и без Node, зеркальные клики в 4 последователя — 0 окон; прямой клик в последователя — 0).
- Коммиты: Task 3 `c61788f`, Task 4 `dd4cb45`.

## Task 5 — Kebab девайса + per-device Vision ✅

- **Сделано:** подменю **Vision ▸** в kebab ⋯ (radio: Inherit global — с подсказкой, что сейчас глобально, · None · 6 типов); shadcn `DropdownMenuSub/SubTrigger/SubContent` добавлены в `ui/dropdown-menu.tsx`. Индикатор: чип **EyeIcon + label** в шапке девайса появляется только при override («Deuteranopia» / «No vision filter» для явного none), tooltip объясняет, клик по чипу = один шаг назад к Inherit. Main-часть была готова в Task 1 (`emulation:set-device-vision`, приоритет override над профилем, persist в `emulation.deviceVision`).
- **Проверено:** e2e `vision.spec.ts` — сьют из двух близнецов 393×852@3 (iPhone 15 Pro + iPhone 15) на фикстуре красный/зелёный; средний цвет `Page.captureScreenshot` (декод через `nativeImage` в main, без PNG-зависимостей): до — одинаковые; Deuteranopia через kebab у второго → сдвинулся только он (Δ > 8), первый без изменений (Δ ≤ 2); клик по чипу → оба снова одинаковые. 1/1.
- **Цифры:** юниты без изменений (стор покрыт в Task 1), e2e +1.
- Коммит `6531904`.

## Task 6 — Диагностика: console errors + overflow ✅

- **Сделано:** `main/diagnostics.ts` (DiagnosticsManager): `Runtime.enable` на каждой вьюшке (через `CDPController.enableRuntime`, реплей после реаттача), счётчик `Runtime.exceptionThrown` + `consoleAPICalled` type error/assert с момента последней навигации (`Runtime.executionContextsCleared` — сброс), последние 20 сообщений по 200 символов; overflow-скан — одноразовый `Runtime.evaluate` (`CDPController.evaluate`, returnByValue) после `did-finish-load` и повтор через 1 с (lazy-контент) + после смены метрик девайса: `scrollWidth > clientWidth` и до 10 виновников outermost-first (вложенные в уже найденный пропускаются) с label `tag#id.class` и селектором `tag#id` / `body > … > tag:nth-of-type(n)`. Ответ страницы валидируется по полям (`parseScan`), **селекторы не покидают main** — renderer подсвечивает по индексу (`diagnostics:highlight [deviceId, index|'all'|'none']`). Подсветка — insertCSS-слой `outline` (скроллится с элементом, несколько сразу), один слой на девайс. Батч-событие `diagnostics` в `MainEvent` через deferrer по образцу load-state-batcher; `diagnostics:get` для только что стартовавшего рендерера. `devtools:open` получил опциональный `'console'`. Renderer: `stores/diagnostics.ts`, `DiagnosticsChips.tsx` — чипы **N errors** (красный; tooltip с последними 5 строками; клик → DevTools console девайса) и **overflow** (янтарный; popover со списком, hover — подсветка, «Highlight all», закрытие снимает подсветку) слева от Mirror.
- **Решения:** скан в main world, а не в isolated world (план допускал оба): `Page.createIsolatedWorld` требует frameId и пересоздание после каждой навигации, а результат и так валидируется как недоверенные данные и не несёт ничего, кроме геометрии; ничего постоянного не инжектируется. Highlight — CSS, а не `Overlay.highlightNode`: не требует `DOM.enable`, подсвечивает несколько элементов и скроллится.
- **Грабля (e2e):** под mobile-эмуляцией страница шире вьюпорта **ужимается** (shrink-to-fit как на телефоне): `window.innerWidth` становится шириной контента (1012), layout viewport `documentElement.clientWidth` остаётся 393 — вьюшки в e2e идентифицировать по `clientWidth`. Перф `Runtime.enable` × 10 — замер в Task 11.
- **Цифры:** юниты +34 (diagnostics 22, validate 3, cdp 3, store 4 …); e2e `diagnostics.spec.ts` 1/1 (2 errors на всех 5, overflow ровно на 3 узких, popover с `div#wide.banner.promo` 1000px, Highlight all → outline solid только в этой странице, Escape снимает, reload обнуляет счёт).
- Коммит `15bd75f`.

## Task 7 — Линейки и направляющие ✅

- **Сделано:** линейки (20px, canvas 2D, `lib/rulers.ts`: шаг делений по зуму — 100 CSS-px при 100%, 200 при 50%, 50 при 200%; начало отсчёта = scroll offset страницы) — DOM **вокруг** измеряемого бокса (`Rulers.tsx`, рамка растёт на полосу с двух сторон, `data-rulers="on"`); линии — CSS-слой в странице (`main/guides.ts`: `html::after` absolute от начала документа, linear-gradient на позицию каждой направляющей, `!important`, `z-index` max, `pointer-events:none`; **размер слоя = измеренные `clientWidth × scrollHeight`** — абсолютный бокс выше документа удлинил бы страницу; переизмерение на `did-finish-load` и при каждом изменении набора). Интеракция целиком на полосе: клик — направляющая в этой позиции (и сразу drag), drag маркера — перемещение (маркер трекается **по значению**, т.к. стор пересортировывает ось), уход с полосы на 24px или double-click — удаление, подпись значения следует за курсором. Persist по ключу `WxH` (`guides` в документе, sanitize/validate: ≤ 50 на ось, целые 0..100000, ≤ 100 размеров), запись с debounce 250 мс в рендерере + 300 мс в main. Rulers on/off — session-режим (не персистится). Kebab «Rulers» (Alt R), ⋯ → **Debug ▸ Rulers on all devices**, хоткей `Alt+R` (лид, иначе все).
- **Скролл без нового потока:** preload добавил `x/y` (px) в тот же scroll-сэмпл (одна rAF-пачка на кадр); `SyncEngine.setReporting(deviceId)` держит capture у девайса с линейками даже когда он не лидер; `onScroll` отдаёт последний сэмпл пачки до gating зеркалирования; main коалесирует в `scroll-state` (по образцу load-state, только для девайсов с линейками); `rulers:set` отвечает текущей позицией (одноразовый `Runtime.evaluate`), чтобы линейка не стартовала с нуля.
- **Проверено e2e** (`rulers.spec.ts`, пиксели через `nativeImage`): клик на 100 → `guides['393x852'].v=[100]` и синий столбец x=100 (белые 60/140) в iPhone; десктоп 1440×900 — без направляющих, белый; зум 50% → клик на 100 экранных = 200 страничных, линия на 200; рестарт → документ `{h:[],v:[100,200]}` и линии на странице сразу после включения линеек.
- **Грабли e2e:** `documentElement.clientWidth` у десктопа = 1425 (классический скроллбар 15px) — идентифицировать вьюшку по `innerWidth` или `clientWidth`; `[data-device-id]` теперь и у canvas-линеек — локатор `div[data-device-id]`.
- **Цифры:** юниты +45 (rulers 12, guides store 11, guides manager 9, sync 2, validate 7, persistence 6 …); e2e +1.
