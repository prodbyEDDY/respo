# Шаблон промпта агента-исполнителя

> Копируй этот файл как `W<N>-AGENT-<буква>-<тема>.md` (волна) или
> `TASK-YYYY-MM-DD-<тема>.md` (одиночная задача). Заполняй все секции. Пустые — удаляй.
> Цель — чтобы исполнитель (человек или CC-инстанс) сделал задачу без доуточнений.

---

# W<N> · Агент <буква> — <название задачи>

**Ветка:** `feature/<...>` · **Дата:** YYYY-MM-DD
**Статус:** ⬜ не начато
**ROADMAP:** ссылка на пункт §10

## 0. Контекст

Respo — десктопный инструмент адаптивной веб-разработки (Electron + WebContentsView +
React + TS; страница в N девайс-вьюпортах с синхронизацией). Перед работой прочитай:
- `C:\CODING\My Coding Projects\Respo-Design\CLAUDE.md` — жёсткие правила.
- Спека `docs/superpowers/specs/2026-08-28-respo-design.md` — релевантные секции области.
- `docs/ROADMAP.md` §10 — где эта задача в общем треке.

Кратко о задаче: <1–2 предложения — что и зачем>.

## 1. Границы файлов (zero-overlap)

| Можно трогать | НЕ трогать |
|---|---|
| `<...>` | всё остальное; особенно `src/main/` (ViewManager/CDPController/SyncEngine) вне своей задачи |

Если нужна проводка в `src/shared/ipc.ts` — не редактируй его:
сдай готовый wiring-сниппет (тип канала + payload) в финальном отчёте.

## 2. Что сделать

- [ ] <конкретный пункт>
- [ ] <конкретный пункт>

**Критерий готовности:** <как понять, что задача закрыта>.

## 3. Конвенции (соблюдать)

- Лицензии зависимостей: только MIT/Apache-2.0/BSD/ISC; GPL/AGPL и любой код responsively-app запрещены.
- Только WebContentsView (`<webview>`/BrowserView запрещены); «браузерные» фичи — через CDP.
- Никаких пер-событийных IPC-потоков: rAF-коалесинг, батчи, очереди (перф-бюджеты — спека §8).
- Безопасность: sandbox + contextIsolation, валидация URL, разрешения «спрашивать» (спека §7a).
- IPC-каналы — только через типы из `src/shared/ipc.ts`; Zustand (не Redux); TS strict.
- UI: shadcn/ui + Tailwind + Heroicons, тексты UI — английский.
- <добавить специфичные для задачи правила>

## 4. Проверка

- [ ] `npm run typecheck` — без ошибок.
- [ ] `npm test` — юниты зелёные (новая логика покрыта).
- [ ] `npm run dev` — живой запуск, фича работает руками.
- [ ] `npm run e2e` — при изменениях сквозных сценариев.
- [ ] Ручная проверка: <шаги>.

## 5. Документация (часть Definition of Done)

- [ ] Обновлён профильный документ в `docs/` (и спека, если изменилось архитектурное решение).
- [ ] Координатору: запись в `docs/ROADMAP.md` §10/§11.

## 6. Финал

Не мержить без ревью координатора. Сообщить: что сделано, какие файлы затронуты,
какие риски/вопросы остались, wiring-сниппеты для `src/shared/ipc.ts` (если были).
